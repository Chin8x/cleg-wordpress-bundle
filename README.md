# CLEG WordPress Bundle Delivery

This public repository contains the generated WordPress bundle and its SHA-256 checksum. The root keeps only the canonical bundle, the fixed loader alias, and the current versioned copy. Historical generated bundles are preserved under `old/` for recovery.

Source modules, documentation, and deployment credentials remain in the private `Chin8x/field-workforce-saas` repository.
